# 测试基线 2026-07-10（阶段 0 收口）

> 目的：为 Executable NAV 开发建立可复现基线。默认 marker/deselect 规则未改动。

## 运行环境

| 项 | 值 |
|---|---|
| Commit | `67bb891871bfed107add67c53727e731403d8381`（main，= origin/main） |
| 工作区 | modified: `task_plan.md`（+8 行，文档）；其余为 untracked（见 `workspace_inventory_20260710.md`） |
| Python | 3.11.15（conda env `quantmind`，`/home/lenovo/miniforge3/envs/quantmind/bin/python`） |
| pytest | 9.0.3（plugins: timeout-2.4.0, cov-7.1.0, asyncio-1.3.0, mock-3.15.1, xdist-3.8.0） |
| pandas / numpy | 2.3.3 / 2.0.2 |
| lightgbm / scikit-learn | 4.6.0 / 1.8.0 |
| pyarrow | 23.0.1 |
| fastapi / streamlit | 0.136.1 / 1.57.0 |

## 命令

```bash
/home/lenovo/miniforge3/envs/quantmind/bin/python -m pytest tests/ --no-cov -q --tb=short
```

（marker 过滤沿用 `pyproject.toml` addopts：`-m "not requires_network and not requires_db and not requires_optional_deps and not stale_panel_fixture"`）

## 结果

| 指标 | 值 |
|---|---|
| collected | **1189**（1122 selected + 67 deselected） |
| **passed** | **1121** |
| failed | **0** |
| skipped | 1（`test_pit_correctness.py` 内 1 项） |
| deselected | 67（marker 规则：network 7+ / db 3 / optional_deps / stale_panel_fixture 10 等，登记见 `test_markers.md`） |
| warnings | 38（第三方 deprecation 为主：pydantic 2.11、mlflow filestore、transformers tokenizer 等，无本仓库错误） |
| 总耗时 | **216.58s（3:36）** |

## 失败测试

无。

## 与 Phase 1 合并时基线对比

Phase 1 闭幕（`phase1_closure.md`，2026-06-19）记录为 1189 collected / 1121 passed / 1 skipped / 67 deselected / 0 fail。
**本次结果与之完全一致** → main 自合并以来无回归；可作为 Executable NAV 开发前基线。

## 已知登记债务（非本次失败，不掩盖）

- 10 个 `stale_panel_fixture` 测试：依赖陈旧 panel fixture，与 `stash@{0}`（panel/model refactor）绑定，解封时一并修复（`test_failure_triage_2026Q2.md`）。
- 7+ `requires_network`、3 `requires_db`：环境依赖测试，默认排除，按需 `-m` 单独跑。
